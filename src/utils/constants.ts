export enum STAGES {
	INCIDENT = "incident",
	"CONTRIBUTORY FACTOR/CAUSE" = "contributory_factor_cause",
	OUTCOME = "outcome",
	TRIGGERS = "triggers",
	"FOLlOW UP" = "follow_up",
	PATIENT = "patient",
	REPORTER = "reporter",
}

export enum USER_TYPES {
	USER = "user",
	SUPER_ADMIN = "super_admin",
}
